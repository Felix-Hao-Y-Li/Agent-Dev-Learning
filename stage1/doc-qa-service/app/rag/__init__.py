"""rag 业务层 —— 项目真正干活的地方。

白话说：切文档、存进库、按问题找资料、让模型写答案，全在这一层。
好比餐厅的后厨。

这一层有一个硬规矩：不认识 HTTP。
这里没有一行 FastAPI 的代码，不知道请求头、状态码是什么。
所以同一套问答逻辑，网页接口能调，命令行能调，测试里也能直接调。

离线用（入库时）：
    chunking.py    读文档、切成小块、给每块起一个固定不变的名字
    embeddings.py  把文字翻译成一串数字，意思相近的文字翻译出来的数字也相近
    store.py       向量库，存那些数字，能很快找出最接近的几条

在线用（回答问题时）：
    prompt.py      给模型定规矩，并把查到的资料排好版
    pipeline.py    把「查资料 → 写答案 → 核对出处」串成一条线

详细说明见项目根目录的 FLOW.md 第零章。
"""
