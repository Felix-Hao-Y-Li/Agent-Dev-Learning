"""schemas 契约层 —— 规定接口收什么、回什么。

白话说：一份对外承诺书。好比餐厅的菜单。
调用方照着它写代码，只要它不变，我们内部怎么改都不影响对方。

FastAPI 会根据这一层自动做三件事：
检查请求合不合法、生成 /docs 接口文档、把返回值转成 JSON。

    ask.py  问答接口的请求格式、出处格式、响应格式

详细说明见项目根目录的 FLOW.md 第零章。
"""
