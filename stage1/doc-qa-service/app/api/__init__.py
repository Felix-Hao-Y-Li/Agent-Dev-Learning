"""api 接口层 —— 和外面的世界打交道。

白话说：收 HTTP 请求，交给业务层，把结果打包成 HTTP 响应。
好比餐厅的前台服务员：接单、递给后厨、把菜端出去，自己不做菜。

这一层应该很薄。
如果发现这里的代码越写越长，通常说明业务逻辑漏到了不该在的地方。

    middleware.py      门口登记员。每个请求进门发编号，出门记日志
    deps.py            把开门时建好的后厨对象递给接口函数
    routes/health.py   两个体检接口，给部署平台用
    routes/ask.py      问答接口

详细说明见项目根目录的 FLOW.md 第零章。
"""
