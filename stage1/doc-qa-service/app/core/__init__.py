"""core 地基层 —— 配置、日志、错误。

白话说：所有地方都要用、但和「问答」本身无关的东西，放在这一层。
好比一家餐厅的水电和账本，每个人都要用，但它不参与做菜。

这一层谁都不依赖，是最底下那块砖。
api 层和 rag 层都可以用它，它不可以反过来用那两层。

    config.py   读配置。把 .env 里的设置变成 Python 对象，写错了启动就报错
    logging.py  记日志。每行日志是一个 JSON，并自动带上当前请求的编号
    errors.py   处理错误。不管哪里出错，调用方收到的错误格式都一样

详细说明见项目根目录的 FLOW.md 第零章。
"""
