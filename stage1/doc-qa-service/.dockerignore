# 构建镜像时不要把这些内容送进去。
# 作用有两个：镜像更小、构建更快；以及避免把密钥打进镜像。

# 本地虚拟环境。容器里会重新装一份 Linux 版的依赖，宿主机这份带的是 Windows 二进制，
# 拷进去不但没用，还会几 GB 地拖慢构建。
.venv/

# 密钥绝对不能进镜像。镜像会被推到仓库、被别人拉取，
# 打进去等于公开。运行时用 env_file 注入。
.env

# 向量库是可再生的产物，由 ingest 命令生成，运行时挂载卷。
chroma_db/
chroma_db_*/

# 版本库和开发产物
.git/
.gitignore
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.vscode/

# 测试不进生产镜像
tests/
